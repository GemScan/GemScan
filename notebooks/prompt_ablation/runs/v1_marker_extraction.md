# v1 Marker Extraction Framing

Prompt artifact: `../prompts/v1_marker_extraction_prompts.md`

Purpose: move away from asking the model for a final scam verdict and instead
extract observable scam markers that can feed deterministic scoring.

Coverage:

- E2B 8-bit: attempted in imported scratch work.
- E2B 4-bit: attempted in imported scratch work.

GitHub decision: keep as the first major ablation step. The full notebooks and
review-by-image scratch files are archived because they are large and not needed
for a normal repository review.

